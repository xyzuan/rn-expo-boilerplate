import React from "react";
import { Stack } from "expo-router";

const BlogLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default BlogLayout;
