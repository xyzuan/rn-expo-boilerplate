import { iconCn } from "@/lib/utils";

import { Copy, HomeIcon, User, Info } from "lucide-react-native";

iconCn(Copy);
iconCn(HomeIcon);
iconCn(User);
iconCn(Info);

export { Copy, HomeIcon, User, Info };
